# PostAward media

`excel-icon-17.png` is the Excel glyph used on Register and Detail (same asset as ProcTrack).

It is injected into `PostAward.msapp` and `app.msapp` as media name `excel-icon-17`.

If the icon is missing after import in Studio: **Media → Upload image** and name it exactly `excel-icon-17`.
